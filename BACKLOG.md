# Backlog улучшений (зафиксировано@2026-07-09, первая боевая сессия)

Известные слабости и идеи — очередь для сессий улучшения скиллов. Вычёркивай сделанное.

## 🔴 Дыры

- **Клоны-щель в дедупе**: работодатель публикует ту же вакансию новым id ПОСЛЕ нашего отклика (DataLouna ×11) — пройдёт префильтр как новая, hh `alreadyApplied` не сработает (другой vacancy_id). Фикс: писать пары (company, normalized name) отправленных в applied.json и резать совпадения в `scan.py::prefilter`.
- **inbox.py не видит «state изменился»**: скилл hh-inbox предписывает обрабатывать смену state с прошлого прогона, но механики нет — нужен снапшот предыдущего inbox.json и diff (сейчас триггерит только unread).

## 🟡 Качество

- **Расширенные запросы = 47% мусора** (сессия@2026-07-12: 20 доп. запросов → 250 kept, из них 118 score=0 — «руководитель разработки»/«cto»/«техлид» тянут стройку/маркетинг/HR). Но они же дали все 3 семёрки дня (golang→PT, llm→ПРАЙД, техлид→Инфогород), которых базовые QUERIES не видели. Идеи: добавить в scan.py постоянные запросы golang/llm/техлид; расширить STOP_NAME (бухгалтер, юрист, маркетинг, стройк, HR, рекрутер) — резать мусор до LLM-скоринга.

- **sonnet-скрин щедрит на границе 6–8** (первый прогон: из топ-25 восьмёрок ~7 пришлось понижать — Python-primary под видом fullstack, аутстафф, DS-роли). Идеи: ужесточить промпт (явные анти-паттерны), или скорить двумя независимыми sonnet и брать min, или порог отправки поднять до 8 при батчах >200.
- **28 писем первого дня ушли монолитами** — правила humanize (профиль: свежий опыт приоритетно, абзацы, ≤3 цифры) появились после. Проверить конверсию: если ответов мало — возможная причина.
- **Резюме на hh (6 шт.) не проверены на humanize/абзацы** — юзер просил «проверяй и в самом резюме». Отдельный заход: браузер-ревью текстов резюме с правками на согласование.
- **Калибровка писем по результатам**: когда накопятся ответы (applied.json: status rejected/interview/replied) — сопоставить стиль/hooks с исходами, скорректировать правила в profile.md.

## 🟢 Идеи

- **Гипотеза (наблюдение@2026-07-12, НЕ проверено):** «тесты» двух вакансий оказались скрининг-вопросами на странице `/applicant/vacancy_response` (submit `vacancy-response-submit-popup`), у одной была кнопка «Откликнуться без теста». Возможно, часть testPresent-вакансий откликается POST'ом с полями анкеты или вовсе без теста. Проверить прежде чем менять apply.py/скилл.

- **Cron-режим**: ежедневный утренний скан+скоринг+отчёт (без отправки), отклики — по подтверждению из отчёта. Через `/schedule` или systemd-timer + headless-запуск.
- **chatik: настоящий API**: полная история чата и отправка REST'ом не найдены (SSR-конфиг отдаёт только lastMessage; remote.chatik JS — лоадер, реальные чанки не грепал). Найти XHR/WS-протокол → уйти от Playwright в inbox-отправке.
- **Simplenight-паттерн**: вакансии «только рабочие руки, не менеджер» — скоринг мог бы детектить и адаптировать письмо (меньше лид-акцента). Сейчас handled вручную в письме.
- **hh-test-apply: батч-режим** — сейчас одна сессия = один субагент на все вакансии; при >5 тестов подряд возможен таймаут субагента, подумать о чанках.
- **applied.json растёт** — при сотнях записей дедуп-чтение всё ещё дёшево, но письма в логе разбухают; подумать про отсечение letter в архив.

## Хвосты первого дня (разовые)

- ⏳ Google-форма «Это Легко» — юзер сам (логин + семейное положение/дети): https://forms.gle/nttDTbWitsTtb4Jc7
- ⏳ Simplenight ручной отклик (тест): https://hh.ru/vacancy/134015457
- ⏳ Бизнес Технологии: дослать письмо в чат по запросу юзера (`/hh-inbox`)
